[project]
name = "hemo1d"
version = "0.1.0"
description = "1D hemodynamics solver with FEniCSx"
requires-python = ">=3.10"
dependencies = []

[tool.pytest.ini_options]
testpaths = ["tests"]

[tool.ruff]
line-length = 100
target-version = "py310"
